[project]
name = "fab-risk-radar"
version = "0.1.0"
description = "Monthly tier-2/3 semiconductor disruption risk report."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", "src"]

[tool.ruff]
line-length = 100
target-version = "py311"

